SET TALK OFF
SET PATH TO C:\LACTO2006\LX43
CLOSE DATABASES
USE CLIN ORDER 2 IN 1 EXCLUSIVE
USE CALOR ORDER 2 IN 2

SELECT CLIN
ZAP

SELECT CALOR
SCAN 
	IF CALOR.STAT='TRATA'
	SELECT CLIN
	APPEND BLANK
		REPLACE CLIN.ID WITH CALOR.ID
		REPLACE CLIN.NP WITH CALOR.NP
		REPLACE CLIN.FECHA WITH CALOR.FECHA
		REPLACE CLIN.CLAVE WITH 8
		REPLACE CLIN.STAT WITH 'TRATA'
		REPLACE CLIN.TRAT WITH CALOR.OBSC
		REPLACE CLIN.MEDH WITH CALOR.MEDP
		REPLACE CLIN.ENF WITH 0
		
	SELECT CALOR
	ENDIF	
ENDSCAN

SELECT CLIN
BROWSE FIELDS ID,NP,FECHA,CLAVE,STAT,BACT,MEDH,TRAT
CLOSE DATABASES 
